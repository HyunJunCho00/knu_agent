name: Daily Notice Crawler

on:
  schedule:
    # 한국 시간 00:00 = UTC 15:00
    - cron: '0 15 * * *'
  workflow_dispatch: # 수동 실행 버튼 (테스트용)

permissions:
  contents: write

jobs:
  crawl:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Crawler
        env:
          # 파이썬이 app 모듈을 찾을 수 있게 경로 설정
          PYTHONPATH: ${{ github.workspace }}
        run: |
          echo "🕷️ Starting Crawler..."
          # 크롤러 실행 (설정 파일 덕분에 data/ 폴더에 자동 저장됨)
          python app/crawling/crawl_notice.py

      - name: Commit and Push changes
        run: |
          git config --global user.name 'KNU Bot'
          git config --global user.email 'actions@github.com'
          
          # 1. 데이터 폴더 확인
          ls -R data/
          
          # 2. 변경된 데이터 파일 강제 추가 (.gitignore 무시)
          # (knu_master.jsonl 등 생성되는 파일명에 맞춰 수정 가능)
          git add -f data/*.jsonl
          git add -f data/*.json
          
          # 3. 변경사항이 있을 때만 커밋 및 푸시
          git commit -m "📅 Daily Update: $(date +'%Y-%m-%d')" || echo "No changes to commit"
          git push